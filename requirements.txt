# Web Framework
Flask==3.1.3

# Authentication & Security
Flask-Login==0.6.3
Flask-WTF==1.2.1  # CSRF protection for forms + AJAX
bcrypt==4.1.2

# Database Migrations
alembic==1.18.4
psycopg2-binary==2.9.10  # PostgreSQL support (2.9.10 = first with cp313 wheels; fixes CI on Python 3.13)

# OCR i przetwarzanie PDF
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==12.2.0
opencv-python>=4.8.0
numpy>=1.24.0
PyMuPDF>=1.24.0  # For direct text extraction from text-based PDFs

# Eksport danych
openpyxl==3.1.5
pandas>=2.0.0

# Import wizyt z URL (caldis.pl — metoda requests)
requests>=2.28.0
beautifulsoup4>=4.12.0

# Import wizyt z URL (caldis.pl — metoda Playwright/Chromium)
# Jednorazowa instalacja przeglądarki: python -m playwright install chromium
playwright>=1.40.0

# Parsowanie i walidacja
python-dateutil==2.9.0.post0
schwifty==2026.3.0

# SMS — Twilio outbound reminders
twilio>=9.0.0

# Background job scheduler (SMS auto-send)
APScheduler>=3.10.0

# Utilities
python-dotenv==1.2.2

# Production WSGI server (Render/Linux deployment)
gunicorn==21.2.0