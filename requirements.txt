# Web Framework
Flask==3.1.3

# Authentication & Security
Flask-Login==0.6.3
bcrypt==4.1.2

# Database Migrations
alembic==1.18.4
psycopg2-binary==2.9.10  # PostgreSQL support (2.9.10 = first with cp313 wheels; fixes CI on Python 3.13)

# QR PNG rendering (mobile presence confirmation, see below)
Pillow==12.2.0

# Eksport danych
openpyxl==3.1.5

# Import wizyt z URL (caldis.pl — metoda requests)
requests>=2.28.0
beautifulsoup4>=4.12.0

# Parsowanie i walidacja
python-dateutil==2.9.0.post0
schwifty==2026.3.0

# Utilities
python-dotenv==1.2.2

# Mobile presence confirmation (MOBILE_PRESENCE_CONFIRMATION_PLAN.md) —
# rate-limiting the public sign-in endpoints; QR PNG rendering rides the
# Pillow installed above.
Flask-Limiter>=3.8,<4.0
qrcode>=7.4,<8.0

# Redis client for Flask-Limiter's storage backend (SCALING_PREP_PLAN.md
# Phase 4). flask-limiter speaks redis:// once this package is importable —
# no flask-limiter[redis] extra needed. Unused until RATELIMIT_STORAGE_URI
# is set; default stays 'memory://' (see extensions.py).
redis==5.2.1

# Structured JSON logging (SCALING_PREP_PLAN.md Phase 5) — config/logging_config.py.
python-json-logger==3.2.1

# Production WSGI server (Render/Linux deployment)
gunicorn==21.2.0
