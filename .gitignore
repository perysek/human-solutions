# Environment / secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-py312/
.pytest_cache/

# Node / frontend
frontend/node_modules/
frontend/dist/

# IDE
.idea/
.vscode/

# Claude Code — settings.json is shared/team config, settings.local.json is
# per-developer (same idea as .env vs .env.local).
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Legacy source data (Phase 8 migration input) — real production HR
# records, never belongs in the repo. See database.db in the working tree.
*.db

# Logs
*.log

# Playwright MCP scratch output (screenshots, console logs)
.playwright-mcp/

# Git Bash crash dumps — seen recurring, harmless, from bash.exe itself on
# this Windows dev setup, not from anything in this repo.
*.stackdump
